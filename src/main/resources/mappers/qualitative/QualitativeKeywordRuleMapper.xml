<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
    PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
    "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.ohgiraffers.team3backendbatch.infrastructure.persistence.qualitative.mapper.QualitativeKeywordRuleMapper">

    <select id="findActiveDomainKeywordRules"
            resultType="com.ohgiraffers.team3backendbatch.infrastructure.persistence.qualitative.dto.DomainKeywordRuleRow">
        SELECT
            domain_keyword AS keyword,
            domain_base_score AS baseScore,
            domain_weight AS weight
        FROM domain_keyword
        WHERE domain_is_active = TRUE
          AND domain_keyword IS NOT NULL
          AND CHAR_LENGTH(TRIM(domain_keyword)) > 0
        ORDER BY domain_keyword_id DESC
    </select>
</mapper>